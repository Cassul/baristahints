<h1><%= @hint.title %></h1>
<p><%= @hint.body %></p>
<img src="<%= @hint.image_url%>">
<br>
<%= link_to "<< Back to all hints", hints_path %>
<br>

<%= link_to "edit", edit_hint_path(@hint) %>

<%= link_to "delete", hint_path(@hint), method: :delete %>